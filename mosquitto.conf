# Listen for MQTT clients on port 1883
listener 1883
protocol mqtt

# Allow anonymous access for testing (can be removed for production)
allow_anonymous true

# Log everything to stdout for debugging
log_type all
log_dest stdout

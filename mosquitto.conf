# Bind to the Render-provided port
listener ${PORT}
protocol mqtt
bind_address 0.0.0.0

# Optional: Enable WebSocket support (Render-friendly)
listener 9001
protocol websockets

# Allow anonymous access for testing (remove for production)
allow_anonymous true

# Log everything to stdout for debugging
log_type all
log_dest stdout
